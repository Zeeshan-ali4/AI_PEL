# Architect Brief — T04: Action normaliser

## Task selected
- Task: T04 — Action normaliser
- Current status: To do
- Dependencies checked: pass — T04 depends on T03, and T03 is marked `Done` in `TASK_LEDGER.md`. T02 and T01 are also marked `Done`, so the Action schema and scenario simulator prerequisites are present.

## Source-of-truth references
- MASTER_SPEC.md: §1 item 2 (normalise proposed actions into one governance schema); §3 logical architecture (`Action normaliser` after PEP); §5.1 canonical `Action` schema; §7 six scenario action types and expected downstream outcomes; §10 canonical file layout; §11 pipeline order step 2.
- TASK_LEDGER.md: Phase 2, T04 — Action normaliser; dependency on T03; allowed files; done criteria; verify guidance; reviewer focus.
- AGENTS.md: work on exactly one task; touch only files listed for the current task plus the test file specified later by PM/BA; do not create extra files outside the task scope; preserve schemas and file layout; every task must produce committed pytest tests; do not mark T04 `DONE` unless verification passes.

## Allowed files
- `app/normaliser/normaliser.py`
- `tests/T04_normaliser/`

## Implementation objective
Implement the action normaliser for T04 only. It must convert intercepted raw tool-call dictionaries from the existing T03 scenario simulator / SDK wrapper into valid `Action` schema objects from `app.schemas.action`, using the exact canonical fields in `MASTER_SPEC.md` §5.1. The normaliser owns generation of fresh UUIDv4 values for `action_id` and `correlation_id`, sets the current timestamp, sets `environment="demo"`, preserves/carries the requested `enforcement_mode`, maps supported tool names to the canonical `action_type`, and copies action-specific details into `resource`, `parameters`, `content`, and `recipient` without adding policy decisions or evidence.

## Non-negotiables
- Implement exactly T04; do not modify scenario definitions, schema models, context, semantic, policy, enforcement, audit, web, OPA, or pipeline files.
- Use the existing Action schema exactly as implemented from `MASTER_SPEC.md` §5.1; do not add, remove, or rename schema fields.
- Supported canonical `action_type` values for this task are only `financial.payment.issue` and `communication.email.send`.
- Unknown raw tool names must raise a clear, explicit error; never silently default to an action type.
- `action_id` and `correlation_id` must be generated by the normaliser as UUIDv4 values for each normalised call; do not reuse scenario IDs or raw-call IDs as substitutes.
- `timestamp` must be set by the normaliser and must validate against the Action schema.
- `environment` must be `demo`.
- `enforcement_mode` must be carried through from the raw call when present and must remain schema-valid (`shadow`, `soft`, or `full`). If missing, use the project default already established by existing T03 raw calls if present; otherwise choose the narrow demo-safe default of `shadow` and document that default in code/tests.
- Payment actions must normalise to `financial.payment.issue`; email send actions must normalise to `communication.email.send`.
- Do not introduce decision logic, context lookup, semantic evidence evaluation, policy calls, enforcement decisions, or audit writes in this task.
- Tests must cover all six scenarios normalising successfully to valid Action objects with the correct canonical `action_type`, generated correlation IDs, and clear failure for an unknown tool.

## Verify step
Run the T04 pytest target after implementation:

```bash
docker compose run --rm app pytest -q tests/T04_normaliser
```

If Docker is unavailable in the environment, run the equivalent local pytest command as a secondary check and report the limitation explicitly:

```bash
pytest -q tests/T04_normaliser
```

## Handoff to Implementer
You are the Implementer Agent. Read `briefs/T04_architect_brief.md` and `briefs/T04_test_brief.md`. Implement exactly T04. Touch only the allowed files above plus the test file specified in the Test Brief. Do not start any other task. Report changed files and verification result.
