services:
  app:
    build: .
    container_name: ai_pel_app
    ports:
      - "8080:8080"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: "8080"
      OPA_URL: http://opa:8181
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ai_pel
      POSTGRES_USER: ai_pel
      POSTGRES_PASSWORD: ai_pel
      DATABASE_URL: postgresql://ai_pel:ai_pel@postgres:5432/ai_pel
    depends_on:
      - opa
      - postgres

  opa:
    image: openpolicyagent/opa:latest
    container_name: ai_pel_opa
    command: ["run", "--server", "--addr=0.0.0.0:8181"]
    ports:
      - "8181:8181"

  postgres:
    image: postgres:15
    container_name: ai_pel_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ai_pel
      POSTGRES_USER: ai_pel
      POSTGRES_PASSWORD: ai_pel
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
